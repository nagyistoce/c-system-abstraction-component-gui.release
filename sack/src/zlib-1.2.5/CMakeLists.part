


SET( ZLIB_SRCS  adler32 compress crc32  uncompr deflate trees  
           zutil inflate inftrees  inffast infback 
#           infblock infcodes infutil
   )
   
   if( NOT __ARM__ )
   # under arm we don't really NEED direct FILEIO stuffs.
SET( ZLIB_SRCS ${ZLIB_SRCS}   gzclose gzlib gzread gzwrite )
    endif( NOT __ARM__ )

FOREACH( SRC ${ZLIB_SRCS} )
	LIST(APPEND ZLIB_SOURCE ${ZBASEDIR}/${SRC})
ENDFOREACH()
include_directories( ${ZBASEDIR} )



