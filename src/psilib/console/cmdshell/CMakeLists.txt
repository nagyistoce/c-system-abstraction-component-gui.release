
PROJECT( psi_cmdshell )

ADD_EXECUTABLE( ${PROJECT_NAME} ${FIRST_GCC_PROGRAM_SOURCE} cmdshell.c ${LAST_GCC_PROGRAM_SOURCE} )
string( REPLACE "." "_" TARGET_LABEL ${PROJECT_NAME} )
SET_TARGET_PROPERTIES(${PROJECT_NAME} PROPERTIES COMPILE_FLAGS  "-DTARGET_LABEL=${TARGET_LABEL}" 
                  FOLDER tests
)
TARGET_LINK_LIBRARIES( ${PROJECT_NAME} ${SACK_LIBRARY} )

install_default_dest( ${PROJECT_NAME} )

