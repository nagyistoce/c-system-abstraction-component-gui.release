

include_directories( ${SACK_BASE}/${PBASEDIR} )

set( PNG_SOURCE
	${PBASEDIR}/png  ${PBASEDIR}/pngset  ${PBASEDIR}/pngget  ${PBASEDIR}/pngrutil  ${PBASEDIR}/pngtrans  ${PBASEDIR}/pngwutil
	 ${PBASEDIR}/pngmem  ${PBASEDIR}/pngpread  ${PBASEDIR}/pngread  ${PBASEDIR}/pngerror  ${PBASEDIR}/pngwrite
	  ${PBASEDIR}/pngrtran  ${PBASEDIR}/pngwtran  ${PBASEDIR}/pngrio  ${PBASEDIR}/pngwio )
          
add_definitions( -D__PNG_LIBRARY_SOURCE__ )
